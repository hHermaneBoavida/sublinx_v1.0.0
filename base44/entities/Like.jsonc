{
  "name": "Like",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string",
      "description": "ID do usuario que curtiu"
    },
    "event_id": {
      "type": "string",
      "description": "ID do evento curtido (opcional)"
    },
    "target_user_id": {
      "type": "string",
      "description": "ID do usuario curtido (para curtir perfil)"
    }
  },
  "required": [
    "user_id"
  ],
  "rls": {
    "create": {
      "data.user_id": "{{user.id}}"
    },
    "read": {},
    "update": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}